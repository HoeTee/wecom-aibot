# MCP Tools

## 当前职责

agent 通过 MCP tools 创建和编辑企业微信文档。

## 关键约束

- 文档连续性依赖 `doc_id`、`doc_url`、`doc_name`
- 用户提到“上一个文档”时，后续编辑应复用当前绑定的同一份文档
- tool 使用本身就是 eval 的一部分，不只是最终文本输出

## tool 质量重点

- 选择正确的文档 tool
- 传入正确的文档标识
- 用户没有明确要求新建文档时，后续编辑不应新建文档
